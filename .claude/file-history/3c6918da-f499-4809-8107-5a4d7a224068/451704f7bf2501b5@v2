/**
 * observability.ts - Observability event stub
 *
 * Provides no-op implementations for observability hooks.
 * Replace with real implementation if an observability backend is configured.
 */

/**
 * Send an event to the observability backend (no-op stub).
 */
export async function sendEventToObservability(_event: Record<string, unknown>): Promise<void> {
  // No observability backend configured
}

/**
 * Get current ISO timestamp.
 */
export function getCurrentTimestamp(): string {
  return new Date().toISOString();
}

/**
 * Get the source application identifier.
 */
export function getSourceApp(): string {
  return 'CASA';
}
